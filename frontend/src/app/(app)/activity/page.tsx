"use client";
import { useFinSyncSession } from "@/components/session/FinSyncSessionProvider"; import { Card, EmptyState, PageHeader } from "@/components/ui";
export default function ActivityPage() { const { session } = useFinSyncSession(); const events = session?.proposals.filter((p) => p.status !== "pending") ?? []; return <><PageHeader title="Activity" description="Actions recorded in this browser session." />{events.length ? <Card><ul className="space-y-4">{events.map((p) => <li key={p.id} className="border-b border-white/10 pb-4 text-sm text-slate-400"><span className="capitalize text-white">Proposal {p.status}</span><br />{p.summary}</li>)}</ul></Card> : <EmptyState title="No session activity" description="Approved and rejected ledgr assistant proposals will appear here." />}</>; }
