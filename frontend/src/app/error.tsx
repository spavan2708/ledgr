"use client";
export default function GlobalError({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <main className="grid min-h-screen place-items-center bg-slate-950 px-5 text-center"><div><h1 className="text-3xl font-bold text-white">FinSync hit an unexpected error.</h1><p className="mt-3 text-slate-500">Your planning state was not intentionally changed.</p><button onClick={reset} className="primary-button mt-6">Try again</button></div></main>; }
